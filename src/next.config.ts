// This file is misplaced and intentionally left empty to resolve a configuration conflict.
// The primary Next.js configuration is at the root of the project in next.config.ts.
const config = {};
export default config;
